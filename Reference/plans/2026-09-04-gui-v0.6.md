# GUI 迁移向导 v0.6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 本地 Web GUI 三步迁移向导(选版本→审阅计划→执行),绿色软件目录布局,文件操作收口 fsops,附完整性校验与 doctor。

**Architecture:** 管线从 cli.py 下沉 `pipeline.py`(cli/gui 平级消费);`fsops.py` 统一文件操作门(原子写/类型化异常);`workdir.py` 解析绿色 data/ 布局(frozen 检测,slug 隔离多游戏根);`gui/server.py` FastAPI 薄翻译层(job+SSE+单锁+Host 校验);单文件 HTML 页面。

**Tech Stack:** Python 3.11+ / FastAPI + uvicorn(新增依赖) / 原生 JS / pytest(fastapi TestClient)

**Spec:** `Reference/specs/2026-09-04-gui-design.md`(§7 决策记录与 §9 原型期妥协为实现时必读)

## Global Constraints

- 注释/docstring 全中文;全函数类型标注;pathlib;I/O 显式 `encoding="utf-8"`
- 新依赖仅 `fastapi>=0.115`、`uvicorn>=0.30`(requirements.txt + pyproject.toml 同步)
- 现有 251 测试零回归是每个任务的硬门槛;`ruff check .` 干净
- `PLAN_FORMAT`/`SNAPSHOT_FORMAT` 不变;`TOOL_VERSION` 0.5.0 → 0.6.0(Task 8 统一 bump)
- gui/server.py 不得 import cli 的任何 `_` 前缀函数(分层纪律,spec §2)
- 绝不删除用户文件;覆盖必先 `_conflict_backup/`(沿用既有语义)

---

### Task 1: pipeline.py 管线下沉

**Files:**
- Create: `migration/pipeline.py`
- Create: `tests/test_pipeline.py`
- Modify: `migration/cli.py`(`_run_plan_pipeline` 及 scan/plan/migrate/swap 的编排调用改为消费 pipeline;`_cmd_scan` 的扫描逻辑同样下沉)

**Interfaces:**
- Produces: `scan_version(game_root: Path, version: str, workdir_snapshots: Path) -> Snapshot`(构建+写快照文件)
- Produces: `build_plan(cwd: Path, game_root: Path, src: str, dst: str, *, modpack_swap: bool = False, rescan_dst: bool = False, save: bool = True) -> tuple[MigrationPlan, list]`(返回 plan+compat_warnings;现 `_run_plan_pipeline` 全量语义,`rescan_dst=True` 时先 `scan_version`)
- Produces: `execute_migration(plan: MigrationPlan, src_root: Path, dst_root: Path, ask_yes: set[str], dry_run: bool = False, progress_cb: "Callable[[FileResult], None] | None" = None) -> list[FileResult]`(Task 3 才实现 progress_cb,本任务签名先留 None 默认,内部 `lambda r: None` 兜底)
- Consumes: 现有 `Scanner`/`build_ruleset`(留在 cli)/`Differ`/`Planner`/`Executor`/moddb 函数

注意:`build_ruleset` 依赖 argparse.Namespace——下沉时改为纯参数签名 `build_ruleset(versions, *, exclude: list[str], include: list[str], rule_files: list[Path], mcmig_dir: Path, with_whitelist: bool = False, orphan_rules: list[Rule] | None = None)`,cli 调用处从 args 展开传参(此改动属本任务,cli 行为零变化)。

- [ ] **Step 1: 写失败测试**(从 cli 行为复刻最小闭环)

```python
"""pipeline 模块测试:编排函数直调(不经 CLI)。"""

import json
from pathlib import Path

from migration.pipeline import build_plan, scan_version


def _mk_version(root: Path, name: str, options: str = "fps:60\n") -> Path:
    d = root / "versions" / name
    d.mkdir(parents=True)
    (d / "options.txt").write_text(options, encoding="utf-8")
    return d


def test_scan_version_writes_snapshot(tmp_path):
    game = tmp_path / "game"
    _mk_version(game, "src", "fps:120\n")
    snaps = tmp_path / "snapshots"
    snap = scan_version(game, "src", snaps)
    assert (snaps / "src.snapshot.json").exists()
    assert any(f.path == "options.txt" for f in snap.files)


def test_build_plan_returns_plan_and_saves(tmp_path):
    game = tmp_path / "game"
    _mk_version(game, "src", "fps:120\n")
    _mk_version(game, "dst")
    snaps = tmp_path / "snapshots"
    scan_version(game, "src", snaps)
    scan_version(game, "dst", snaps)
    plans_dir = tmp_path / "plans"
    plans_dir.mkdir()
    plan, warns = build_plan(
        tmp_path, game, "src", "dst",
        mcmig_dir=tmp_path, plans_dir=plans_dir,
    )
    assert plan.summary()["must_migrate"] >= 1
    assert (plans_dir / "src__dst.plan.json").exists()
```

> 实现者注意:上面测试暴露的最终签名以本任务 Step 3 为准——`build_plan(cwd, game_root, src, dst, *, modpack_swap=False, rescan_dst=False, save=True, mcmig_dir: Path, plans_dir: Path, exclude=(), include=(), rule_files=())`。测试按此签名书写,上文示意含糊处以 Step 3 签名为准。

- [ ] **Step 2: 跑测试确认失败** `pytest tests/test_pipeline.py -v`(ModuleNotFoundError)

- [ ] **Step 3: 实现 pipeline.py**

从 cli.py 原样搬移 `_run_plan_pipeline`/scan 构建逻辑为上述三函数(逻辑不改,仅参数化 mcmig_dir/plans_dir);`_cmd_plan`/`_cmd_swap`/`_cmd_migrate`/`_cmd_scan` 改调 pipeline;`build_ruleset` 重签名后 cli 内两处调用点改展开传参。快照/plan 路径:快照仍由 `snapshot_path(cwd, name)` 概念传入(plans_dir/snapshots 目录参数化,snapshot 文件名规则 `f"{version}.snapshot.json"` 不变)。

- [ ] **Step 4: 跑全量确认零回归** `pytest -q && ruff check .`(251 全绿)

- [ ] **Step 5: Commit** `refactor(pipeline): 管线下沉 pipeline.py,cli 改为消费方(build_ruleset 参数化)`

---

### Task 2: fsops.py 文件操作门

**Files:**
- Create: `migration/fsops.py`
- Create: `tests/test_fsops.py`
- Modify: `migration/executor.py`(复制改走 fsops)、`migration/plan.py`(`MigrationPlan.save` 原子写)、`migration/snapshot.py`(快照落盘原子写)、`migration/cli.py`(`_swap_install` 复制改走 fsops)

**Interfaces:**
- Produces: 异常族 `FsOpsError(OSError)` 及子类 `SourceMissingError`/`TargetLockedError`/`PermissionDeniedError`/`DiskSpaceError`(均含 `what: str`/`why: str` 属性,中文)
- Produces: `md5_of(path: Path) -> str | None`(分块读,不可读返回 None)
- Produces: `long_path(p: Path) -> Path`(win32 绝对路径加 `\\?\` 前缀,其余原样)
- Produces: `copy_atomic(src: Path, dst: Path, *, backup_dir: Path | None = None) -> bool`——事务式:dst 存在且 MD5 不同→先备份到 `backup_dir/<rel>`(rel 由调用方传 `rel: str` 参数,签名含 `rel: str`);`src→dst.parent/<name>.mcmig-tmp` 写入;MD5 校验;`os.replace` 换名;任一步失败删 tmp、已备份则回滚 dst 原件;返回 True=发生备份
- Produces: `write_json_atomic(path: Path, payload: dict) -> None`——tmp+replace 写 JSON(utf-8/ensure_ascii=False/indent=2)
- Produces: `clean_stale_tmp(root: Path) -> int`——递归删除 `*.mcmig-tmp`,返回清除数
- Produces: `check_disk_space(dst_root: Path, needed_bytes: int) -> None`——不足抛 `DiskSpaceError`(why 含缺口 MB 数)

- [ ] **Step 1: 写失败测试**

```python
"""fsops 模块测试:原子复制/回滚/原子写/长路径/磁盘检查/类型化异常。"""

import pytest

from migration.fsops import (
    DiskSpaceError, SourceMissingError, clean_stale_tmp, copy_atomic,
    md5_of, write_json_atomic,
)


def test_copy_atomic_basic_and_backup(tmp_path):
    src = tmp_path / "a.txt"; src.write_text("NEW", encoding="utf-8")
    dst = tmp_path / "b.txt"; dst.write_text("OLD", encoding="utf-8")
    bak = tmp_path / "bak"
    backed = copy_atomic(src, dst, backup_dir=bak, rel="b.txt")
    assert dst.read_text(encoding="utf-8") == "NEW"
    assert backed is True
    assert (bak / "b.txt").read_text(encoding="utf-8") == "OLD"


def test_copy_atomic_identical_short_circuit(tmp_path):
    src = tmp_path / "a.txt"; src.write_text("SAME", encoding="utf-8")
    dst = tmp_path / "a.txt"; dst.write_text("SAME", encoding="utf-8")
    assert copy_atomic(src, dst, backup_dir=tmp_path / "bak", rel="a.txt") is False


def test_copy_atomic_missing_source_raises(tmp_path):
    with pytest.raises(SourceMissingError):
        copy_atomic(tmp_path / "ghost", tmp_path / "x", backup_dir=None, rel="x")


def test_copy_atomic_locked_target_rolls_back(tmp_path, monkeypatch):
    """换名失败(模拟锁定)→ 目标保持原内容,备份在场,tmp 清除。"""
    src = tmp_path / "a.txt"; src.write_text("NEW", encoding="utf-8")
    dst = tmp_path / "b.txt"; dst.write_text("OLD", encoding="utf-8")
    bak = tmp_path / "bak"
    import migration.fsops as fs
    def boom(a, b):
        raise PermissionError(13, "另一个程序正在使用此文件")
    monkeypatch.setattr(fs.os, "replace", boom)
    with pytest.raises(fs.TargetLockedError):
        fs.copy_atomic(src, dst, backup_dir=bak, rel="b.txt")
    assert dst.read_text(encoding="utf-8") == "OLD"          # 回滚
    assert (bak / "b.txt").read_text(encoding="utf-8") == "OLD"
    assert not list(tmp_path.glob("*.mcmig-tmp"))            # tmp 清干净


def test_write_json_atomic_roundtrip(tmp_path):
    p = tmp_path / "s.json"
    write_json_atomic(p, {"k": "中文"})
    import json
    assert json.loads(p.read_text(encoding="utf-8"))["k"] == "中文"
    assert not list(tmp_path.glob("*.mcmig-tmp"))


def test_clean_stale_tmp(tmp_path):
    (tmp_path / "x.mcmig-tmp").write_text("half", encoding="utf-8")
    (tmp_path / "sub").mkdir(); (tmp_path / "sub" / "y.mcmig-tmp").write_text("h", encoding="utf-8")
    (tmp_path / "keep.txt").write_text("k", encoding="utf-8")
    assert clean_stale_tmp(tmp_path) == 2
    assert (tmp_path / "keep.txt").exists()
    assert not list(tmp_path.rglob("*.mcmig-tmp"))


def test_check_disk_space_raises_with_gap(tmp_path):
    with pytest.raises(DiskSpaceError) as ei:
        from migration.fsops import check_disk_space
        check_disk_space(tmp_path, needed_bytes=10**15)
    assert "MB" in ei.value.why


def test_md5_of_missing_returns_none(tmp_path):
    assert md5_of(tmp_path / "ghost") is None
```

- [ ] **Step 2: 确认失败** `pytest tests/test_fsops.py -v`(ModuleNotFoundError)

- [ ] **Step 3: 实现 fsops.py**(全部函数与异常;`copy_atomic` 严格按事务序列;PermissionError/OSError 映射到类型化异常,回滚逻辑在 finally 保证 tmp 清除)

- [ ] **Step 4: 接线**——executor `_copy_one/_backup` 改调 `copy_atomic`(保留原单测语义:冲突备份镜像路径、identical 跳过、OSError 容错改为捕获 `FsOpsError`);`MigrationPlan.save` 与 snapshot 落盘改 `write_json_atomic`;`_swap_install` 复制改 `copy_atomic(backup_dir=None)`。跑全量零回归。

- [ ] **Step 5: Commit** `feat(fsops): 文件操作统一门 — 事务式原子复制/回滚/类型化异常;executor/plan/snapshot/swap 全部接入`

---

### Task 3: Executor 进度回调 + 迁移预检增强

**Files:**
- Modify: `migration/executor.py`(progress_cb)
- Modify: `migration/pipeline.py`(execute_migration 落实:tmp 清理/磁盘预检/progress_cb 传递)
- Modify: `migration/cli.py`(`--force` 重跑不再覆盖 executed_at:若 plan.executed_at 已存在,mark_executed 前置检查,统计合并或保持原值——采用「保留首次 executed_at,统计取最新」)
- Test: `tests/test_executor.py`、`tests/test_pipeline.py`

**Interfaces:**
- Produces: `Executor.execute(dry_run: bool = False, progress_cb: Callable[[FileResult], None] | None = None)`(每个 FileResult 产出后同步回调;不传=行为不变)
- Produces: `pipeline.execute_migration(...)` 内置三道预检:`clean_stale_tmp(dst_root)` → `check_disk_space(dst_root, sum(COPY 动作源文件大小))` → 执行

- [ ] **Step 1: 写失败测试**

```python
# tests/test_executor.py 追加
def test_execute_progress_cb_called_per_file(tmp_path):
    """progress_cb 逐文件回调,顺序与结果一致。"""
    src, dst = tmp_path / "s", tmp_path / "d"
    src.mkdir(); dst.mkdir()
    (src / "a.txt").write_text("1", encoding="utf-8")
    (src / "b.txt").write_text("2", encoding="utf-8")
    from migration.executor import Executor
    from migration.plan import ActionRecord, Behavior, Origin, MigrationPlan
    plan = MigrationPlan(src="s", dst="d", generated_at="t", actions=[
        ActionRecord("a.txt", Behavior.COPY, Origin.MUST_MIGRATE, 1, None, None, "high", "t", None),
        ActionRecord("b.txt", Behavior.COPY, Origin.MUST_MIGRATE, 1, None, None, "high", "t", None),
    ])
    seen = []
    Executor(plan, src, dst, lambda _a: True).execute(progress_cb=seen.append)
    assert [r.path for r in seen] == ["a.txt", "b.txt"]
```

```python
# tests/test_pipeline.py 追加
def test_execute_migration_cleans_tmp_and_checks_disk(tmp_path):
    """预检:清理残留 tmp;磁盘不足抛 DiskSpaceError 且零写盘。"""
    import pytest
    from migration.fsops import DiskSpaceError
    from migration.pipeline import execute_migration
    from migration.plan import ActionRecord, Behavior, Origin, MigrationPlan

    game = tmp_path / "game"
    src = game / "versions" / "src"; dst = game / "versions" / "dst"
    src.mkdir(parents=True); dst.mkdir(parents=True)
    (src / "options.txt").write_text("fps:1\n", encoding="utf-8")
    (dst / "stale.mcmig-tmp").write_text("half", encoding="utf-8")

    plan = MigrationPlan(src="src", dst="dst", generated_at="t", actions=[
        ActionRecord("options.txt", Behavior.COPY, Origin.MUST_MIGRATE, 6, None, None, "high", "t", None)])
    results = execute_migration(plan, src, dst, ask_yes=set())
    assert not (dst / "stale.mcmig-tmp").exists()          # tmp 清理
    assert (dst / "options.txt").read_text(encoding="utf-8") == "fps:1\n"

    import migration.pipeline as pl
    monkey_disk = lambda root, needed: (_ for _ in ()).throw(
        DiskSpaceError("磁盘空间不足", f"缺 {needed // (1024*1024)} MB"))
    plan2 = MigrationPlan(src="src", dst="dst", generated_at="t", actions=[
        ActionRecord("options.txt", Behavior.COPY, Origin.MUST_MIGRATE, 6, None, None, "high", "t", None)])
    original = pl.check_disk_space
    pl.check_disk_space = monkey_disk
    try:
        with pytest.raises(DiskSpaceError):
            execute_migration(plan2, src, dst, ask_yes=set())
    finally:
        pl.check_disk_space = original
```

> 实现者注意:磁盘预检若用注入法测试,`execute_migration` 须调用模块级 `check_disk_space`(可被 monkeypatch);上述 monkeypatch 写法用 `pl.check_disk_space = ...` 直接替换,测试可改为标准 `monkeypatch.setattr(pl, "check_disk_space", monkey_disk)` 更干净——两种都接受。

- [ ] **Step 2: 确认失败 → Step 3: 实现 → Step 4: 全量零回归**
- [ ] **Step 5: Commit** `feat(executor/pipeline): progress_cb 文件级回调 + 执行预检(tmp 清理/磁盘空间)`

---

### Task 4: workdir.py 绿色目录布局

**Files:**
- Create: `migration/workdir.py`
- Create: `tests/test_workdir.py`

**Interfaces:**
- Produces: `WorkDir` frozen dataclass:`root: Path`、`snapshots: Path`、`plans: Path`、`rules: Path`、`config: Path`、`green: bool`
- Produces: `resolve_workdir(game_root: Path | None = None) -> WorkDir`:
  - frozen(PyInstaller)/exe 目录下存在 `data/` 或可创建 → 绿色模式:`root=exe_dir/data`;`config=root/config.toml`(TOML: `game_root = "..."`);game_root 给定时 `snapshots=root/<slug>/snapshots`、`plans=root/<slug>/plans`、`rules=root/<slug>/rules.yaml`(slug=游戏根目录名);未给定时先读 config,读不到抛 `WorkdirError(what="未配置游戏目录", why=...")`
  - 否则 → 兼容模式:`root=cwd/.mcmig`,`snapshots=root/snapshots`,`plans=root/plans`,`rules=root/rules.yaml`,`config=root/config.yaml`(YAML `game_root:` 键,兼容现状)
- Produces: `WorkdirError(Exception)`(含 what/why 中文)
- Produces: `WorkDir.save_game_root(path: Path) -> None` / `WorkDir.game_root() -> Path | None`
- Produces: exe 目录不可写 → 抛 `WorkdirError`(what="软件目录不可写", why="请把 mcmig 移动到可写的文件夹后重试")

- [ ] **Step 1: 写失败测试**

```python
"""workdir 模块测试:绿色模式/兼容模式/slug 隔离/不可写报错。"""

from pathlib import Path

import pytest

from migration.workdir import WorkdirError, resolve_workdir


def test_compat_mode_uses_cwd_mcmig(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    monkeypatch.setattr("migration.workdir._is_frozen", lambda: False)
    wd = resolve_workdir(game_root=tmp_path / "game")
    assert wd.root == tmp_path / ".mcmig"
    assert wd.green is False
    assert wd.rules.name == "rules.yaml"


def test_green_mode_layout_and_slug(tmp_path, monkeypatch):
    monkeypatch.setattr("migration.workdir._is_frozen", lambda: True)
    monkeypatch.setattr("migration.workdir._exe_dir", lambda: tmp_path)
    game = tmp_path / "game"
    wd = resolve_workdir(game_root=game)
    assert wd.green is True
    assert wd.root == tmp_path / "data"
    assert wd.snapshots == tmp_path / "data" / "game" / "snapshots"
    assert wd.rules == tmp_path / "data" / "game" / "rules.yaml"
    assert wd.config == tmp_path / "data" / "config.toml"


def test_green_mode_slug_isolates_roots(tmp_path, monkeypatch):
    monkeypatch.setattr("migration.workdir._is_frozen", lambda: True)
    monkeypatch.setattr("migration.workdir._exe_dir", lambda: tmp_path)
    a = resolve_workdir(game_root=tmp_path / "alpha")
    b = resolve_workdir(game_root=tmp_path / "beta")
    assert a.snapshots != b.snapshots


def test_green_mode_game_root_roundtrip(tmp_path, monkeypatch):
    monkeypatch.setattr("migration.workdir._is_frozen", lambda: True)
    monkeypatch.setattr("migration.workdir._exe_dir", lambda: tmp_path)
    wd = resolve_workdir(game_root=tmp_path / "game")
    wd.save_game_root(tmp_path / "game")
    wd2 = resolve_workdir()
    assert wd2.game_root() == tmp_path / "game"


def test_green_mode_unconfigured_raises(tmp_path, monkeypatch):
    monkeypatch.setattr("migration.workdir._is_frozen", lambda: True)
    monkeypatch.setattr("migration.workdir._exe_dir", lambda: tmp_path)
    with pytest.raises(WorkdirError) as ei:
        resolve_workdir()
    assert "游戏目录" in ei.value.what


def test_unwritable_exe_dir_raises(tmp_path, monkeypatch):
    import sys
    monkeypatch.setattr("migration.workdir._is_frozen", lambda: True)
    monkeypatch.setattr("migration.workdir._exe_dir", lambda: tmp_path)
    monkeypatch.setattr("migration.workdir._ensure_writable",
                        lambda p: (_ for _ in ()).throw(OSError(13, "denied")))
    with pytest.raises(WorkdirError) as ei:
        resolve_workdir(game_root=tmp_path / "game")
    assert "不可写" in ei.value.why
```

- [ ] **Step 2: 确认失败 → Step 3: 实现**(`_is_frozen`/`_exe_dir`/`_ensure_writable` 为模块级小函数供测试注入;config.toml 用 `tomllib` 读+手写 TOML 行保存,不引新依赖)
- [ ] **Step 4: 全量零回归 → Step 5: Commit** `feat(workdir): 绿色软件目录布局 — frozen 检测/slug 隔离/兼容 cwd 回退`

---

### Task 5: manifest 完整性校验 + mcmig doctor

**Files:**
- Create: `tools/gen_manifest.py`(生成 `migration/data/manifest.sha256`)
- Create: `migration/data/manifest.sha256`
- Create: `migration/doctor.py`
- Modify: `migration/cli.py`(doctor 子命令)
- Test: `tests/test_doctor.py`

**Interfaces:**
- Produces: `gen_manifest.py`:对 `migration/data/` 下所有 `*.yaml` 生成 `<sha256>  <文件名>` 清单(相对 data/ 的文件名,LF 行尾)
- Produces: `doctor.verify_data_manifest() -> list[str]`(损坏/缺失/多余文件的中文描述行,空列表=完好;manifest 本身缺失时返回单条「清单文件缺失」)
- Produces: `doctor.run_doctor(workdir: "WorkDir | None" = None) -> tuple[bool, list[str]]`(检查项:①数据文件完整性 ②game_root 已配置且存在 ③workdir 可写 ④versions 目录可读 ⑤磁盘剩余空间>1GB;每项一行 `[✅|❌] 项目: 说明`,任一 ❌ 则整体 False)

- [ ] **Step 1: 写失败测试**

```python
"""doctor 模块测试:manifest 校验 + 综合体检。"""

from pathlib import Path

from migration import doctor


def test_verify_manifest_ok():
    assert doctor.verify_data_manifest() == []


def test_verify_manifest_detects_tamper(monkeypatch, tmp_path):
    import migration.doctor as d
    # 篡改:替换一个 yaml 的字节
    real = doctor._data_dir() / "rebuild.yaml"
    backup = real.read_bytes()
    monkeypatch.setattr(Path, "read_bytes", lambda self: b"tampered" if self == real else Path.read_bytes.__get__(self)())
    try:
        findings = d.verify_data_manifest()
    finally:
        real.write_bytes(backup)
    assert any("rebuild.yaml" in line for line in findings)
```

> 实现者注意:上面 monkeypatch Path.read_bytes 的写法脆弱——改用更直接的方式:把 `rebuild.yaml` 内容临时改写(读原文→改→还原,try/finally),断言 findings 非空且含文件名。以此修正版为准。

```python
def test_run_doctor_green_mode(tmp_path, monkeypatch):
    import migration.workdir as wd
    monkeypatch.setattr(wd, "_is_frozen", lambda: True)
    monkeypatch.setattr(wd, "_exe_dir", lambda: tmp_path)
    game = tmp_path / "game"; (game / "versions" / "v1").mkdir(parents=True)
    w = wd.resolve_workdir(game_root=game)
    w.save_game_root(game)
    ok, lines = doctor.run_doctor(workdir=w)
    assert ok is True
    assert len(lines) >= 4


def test_run_doctor_missing_game_root_fails(tmp_path, monkeypatch):
    import migration.workdir as wd
    monkeypatch.setattr(wd, "_is_frozen", lambda: True)
    monkeypatch.setattr(wd, "_exe_dir", lambda: tmp_path)
    w = wd.resolve_workdir(game_root=tmp_path / "ghost-game")
    w.save_game_root(tmp_path / "ghost-game")
    ok, lines = doctor.run_doctor(workdir=w)
    assert ok is False
    assert any("❌" in line for line in lines)
```

- [ ] **Step 2: 确认失败 → Step 3: 实现**(gen_manifest 用 `.venv/Scripts/python tools/gen_manifest.py` 跑一次并提交产物;verify 用 `hashlib.sha256`;doctor 逐项构造行)
- [ ] **Step 4: CLI 接线**:`mcmig doctor` 打印各行 + 退出码(全绿 0/有 ❌ 1);全量零回归
- [ ] **Step 5: Commit** `feat(doctor): manifest 完整性校验 + mcmig doctor 体检命令`

---

### Task 6: gui/server.py — FastAPI 薄翻译层

**Files:**
- Create: `migration/gui/__init__.py`(空)
- Create: `migration/gui/server.py`
- Create: `migration/gui/STRINGS.py`(页面与错误文案字典,spec §9 集中约定)
- Create: `tests/test_gui_server.py`
- Modify: `requirements.txt`/`pyproject.toml`(fastapi/uvicorn)

**Interfaces:**
- Produces: `create_app(workdir: "WorkDir | None" = None) -> FastAPI`(工厂,测试注入临时 workdir)
- Produces: 路由 `GET /api/versions`→`{"versions": [...], "active": "..."}`;`POST /api/plan {src,dst}`→`{"job_id": "..."}`;`POST /api/migrate {src,dst,ask_yes,dry_run}`→`{"job_id": "..."}`;`GET /api/jobs/{id}/events`→SSE
- Produces: job 模型(模块级 `JobStore`:`start(kind) -> Job | None` 单锁,`Job.events: queue.Queue`,`Job.done: bool`);后台线程执行 pipeline 并推送事件(phase/file/done/error 四型,字段同 spec §4 schema)
- Produces: Host 校验中间件(非 `127.0.0.1:端口`/`localhost:端口` → 403)
- Produces: `GET /` 返回 `index.html`(`importlib.resources` 读 `migration/gui/index.html`,Task 7 创建;本任务先放占位 `<h1>mcmig</h1>`,Task 7 替换)

- [ ] **Step 1: 写失败测试**

```python
"""gui/server 测试:API 契约/SSE 序列/单锁/Host 校验/ask_yes 传递。"""

import threading
import time
from pathlib import Path

import pytest
from fastapi.testclient import TestClient

from migration.gui.server import create_app


def _make_game(tmp: Path) -> Path:
    game = tmp / "game"
    for name, text in (("src", "fps:120\n"), ("dst", "fps:60\n")):
        d = game / "versions" / name
        d.mkdir(parents=True)
        (d / "options.txt").write_text(text, encoding="utf-8")
        (d / name.replace("src", "src") + ".json")  # 占位防误删
    return game


def _wait_job_done(client, job_id, timeout=10.0):
    with client.stream("GET", f"/api/jobs/{job_id}/events") as resp:
        events = []
        for line in resp.iter_lines():
            if line.startswith("data: "):
                import json
                events.append(json.loads(line[6:]))
            if events and events[-1].get("type") in ("done", "error"):
                break
        return events


def test_versions_endpoint(tmp_path, monkeypatch):
    import migration.workdir as wd
    monkeypatch.setattr(wd, "_is_frozen", lambda: False)
    monkeypatch.chdir(tmp_path)
    game = _make_game(tmp_path)
    app = create_app(workdir=wd.resolve_workdir(game_root=game))
    client = TestClient(app)
    resp = client.get("/api/versions")
    assert resp.status_code == 200
    assert set(resp.json()["versions"]) >= {"src", "dst"}


def test_plan_and_migrate_job_flow(tmp_path, monkeypatch):
    import migration.workdir as wd
    monkeypatch.setattr(wd, "_is_frozen", lambda: False)
    monkeypatch.chdir(tmp_path)
    game = _make_game(tmp_path)
    w = wd.resolve_workdir(game_root=game)
    client = TestClient(create_app(workdir=w))
    r = client.post("/api/plan", json={"src": "src", "dst": "dst"})
    assert r.status_code == 200
    job = r.json()["job_id"]
    events = _wait_job_done(client, job)
    types = [e["type"] for e in events]
    assert "phase" in types and types[-1] == "done"

    r2 = client.post("/api/migrate", json={"src": "src", "dst": "dst", "ask_yes": []})
    events2 = _wait_job_done(client, r2.json()["job_id"])
    assert events2[-1]["type"] == "done"
    assert (game / "versions" / "dst" / "options.txt").read_text(encoding="utf-8") == "fps:120\n"


def test_single_job_lock(tmp_path, monkeypatch):
    import migration.workdir as wd
    monkeypatch.setattr(wd, "_is_frozen", lambda: False)
    monkeypatch.chdir(tmp_path)
    game = _make_game(tmp_path)
    client = TestClient(create_app(workdir=wd.resolve_workdir(game_root=game)))
    client.post("/api/plan", json={"src": "src", "dst": "dst"})
    r = client.post("/api/plan", json={"src": "src", "dst": "dst"})
    assert r.status_code == 409


def test_host_header_rejected(tmp_path, monkeypatch):
    import migration.workdir as wd
    monkeypatch.setattr(wd, "_is_frozen", lambda: False)
    monkeypatch.chdir(tmp_path)
    game = _make_game(tmp_path)
    client = TestClient(create_app(workdir=wd.resolve_workdir(game_root=game)))
    r = client.get("/api/versions", headers={"Host": "evil.example.com"})
    assert r.status_code == 403


def test_error_response_three_part(tmp_path, monkeypatch):
    import migration.workdir as wd
    monkeypatch.setattr(wd, "_is_frozen", lambda: False)
    monkeypatch.chdir(tmp_path)
    game = _make_game(tmp_path)
    client = TestClient(create_app(workdir=wd.resolve_workdir(game_root=game)))
    r = client.post("/api/plan", json={"src": "ghost", "dst": "dst"})
    assert r.status_code == 422
    body = r.json()
    assert {"what", "why", "details"} <= set(body.keys())
```

> 实现者注意:`_make_game` 里那行 `(d / name + ".json")` 是残留占位,实现时删除;`_wait_job_done` 在 SSE 结束(连接关闭)后也要能返回已收集事件(job 完成后 server 主动 close);单锁测试依赖 job 未完成即二次请求,若本机跑太快导致竞态,可在 `start` 后立即发第二个请求(不过等待第一个完成)——以「第二个请求 409」为准,必要时在 JobStore.start 加 5ms sleep 保证测试确定性。

- [ ] **Step 2: 确认失败 → Step 3: 实现 server.py**

要点:uvicorn 不进测试;SSE 用 `StreamingResponse(media_type="text/event-stream")`,生成器从 `job.events` 队列取消息、`job.done` 且队列空时结束;后台线程跑 `pipeline.scan_version/build_plan/execute_migration`,phase 事件按 `scan_src→scan_dst→plan` / `migrate→done` 推送,文件级经 `progress_cb` 包装;错误统一 `except FsOpsError/WorkdirError/Exception` → error 事件(what/why/details)+ job 结束;plan job 完成时把分组 action 摘要(按 origin 分组的 `{path,reason,behavior,origin}` 列表)塞进 done 事件的 `plan` 字段供②步渲染;`ask_yes: list[str]` → `set` 传 `execute_migration`;`TestClient` 需 `httpx` 依赖加入 dev requirements(fastapi TestClient 基于 httpx;加 `httpx>=0.27` 到 requirements.txt 与 pyproject)。

- [ ] **Step 4: 全量零回归 → Step 5: Commit** `feat(gui): FastAPI 服务层 — job 模型/SSE 事件流/单任务锁/Host 校验/错误三段式`

---

### Task 7: index.html 三步向导页面

**Files:**
- Create: `migration/gui/index.html`
- Create: `tests/gui-manual-checklist.md`

**Interfaces:**
- Consumes: Task 6 全部 API 与 SSE 事件 schema、`STRINGS.py` 文案由页面内嵌(原型期文案直接写 HTML,`STRINGS.py` 供 server 错误文案,两处都集中)
- Produces: 单文件页面,无外部资源(禁 CDN,离线可用),原生 JS fetch+EventSource

**验收行为(以此为准,样式极简即可,语义化 HTML+<style> 内嵌)**:

- 步①:两个 `<select>`(源/目标,来自 `/api/versions`)+开始按钮 → `POST /api/plan`
- 步②:done 事件的 `plan` 分组渲染——组标题(emoji+名+计数)、❓组每行勾选框(默认不勾)、其他组行显 path+reason;👻组脚注;搜索框前端过滤;「执行迁移」按钮把勾选路径放 `ask_yes` → `POST /api/plan` 换 `POST /api/migrate`
- 步③:EventSource 订阅 job;phase 事件更新阶段标签;file 事件更新 `N/M` 计数;error 事件红条(what/why 可展开 details);done 事件显示 summary 与 PCL 提醒文案(从 done 事件 `reminder` 字段取,server 端在 Task 6 done 事件已带);日志面板=事件原文逐条追加(时间戳)
- 断线:EventSource `onerror` 显示「连接断开,刷新页面重新开始」

- [ ] **Step 1: 编写 index.html**(实现者按上述行为清单从零写,允许自主排版;唯一硬约束:零外部资源、UTF-8、中文文案、三步容器 `id="step1/2/3"` 便于手测)
- [ ] **Step 2: 手测清单写入 `tests/gui-manual-checklist.md`**(三步走通/ASK 勾选生效/断线刷新/error 渲染,各 3-5 条,含前置条件与预期)
- [ ] **Step 3: `pytest -q && ruff check .` 全量零回归(html 不进 lint)**
- [ ] **Step 4: Commit** `feat(gui): 三步向导页面(单文件/零外部资源/SSE 实时渲染)+ 手测清单`

---

### Task 8: mcmig gui 子命令 + 文档收尾

**Files:**
- Modify: `migration/cli.py`(`gui` 子命令)
- Modify: `README.zh-CN.md`/`README.en.md`
- Modify: `migration/snapshot.py`/`migration/__init__.py`/`pyproject.toml`(0.6.0)

**Interfaces:**
- Produces: `mcmig gui [--port N] [--no-browser]`:socket 取空闲端口(默认随机)→ threading.Timer(0.8s) `webbrowser.open` → `uvicorn.run(create_app(), host="127.0.0.1", port=port)`;启动前先 `resolve_workdir()`+`verify_data_manifest()`,失败打印 doctor 行退出 2

- [ ] **Step 1: 单测(parser 注册 + 启动前检查失败路径)**

```python
# tests/test_cli.py 追加
def test_gui_subcommand_registered():
    from migration.cli import build_parser
    p = build_parser()
    args = p.parse_args(["gui"])
    assert args.no_browser is False


def test_gui_manifest_tamper_exits_2(tmp_path, monkeypatch, capsys):
    from migration import cli, doctor
    monkeypatch.setattr(doctor, "verify_data_manifest",
                        lambda: ["rebuild.yaml 内容与清单不符"])
    rc = cli.main(["gui", "--no-browser"])
    assert rc == 2
    assert "清单" in capsys.readouterr().out
```

- [ ] **Step 2: 确认失败 → Step 3: 实现 → Step 4: 全量零回归 → Step 5: README 增补**

README 增补(中英同构):命令表加 `mcmig gui`/`mcmig doctor` 两行;新增「数据与卸载」一节(绿色 data/ 布局、游戏侧仅 `_conflict_backup/`、卸载=删文件夹+可选删游戏侧备份、Release SHA256 校验命令 `certutil -hashfile mcmig.exe SHA256`);版本 0.6.0。

- [ ] **Step 6: Commit** `feat(cli): mcmig gui 子命令(随机端口/自动开浏览器/启动自检)+ README 数据与卸载说明;版本 0.6.0`

---

## Self-Review Checklist(实现完成后核对)

1. spec §2 分层:server.py 无 cli 私有函数 import(grep 验证);pipeline 被 cli 与 gui 共同消费
2. spec §3 绿色布局:零 APPDATA/用户目录写入;slug 隔离;只读目录友好报错
3. spec §4:4 API+单锁+Host 校验+两级事件;错误三段式
4. spec §6:copy_atomic 事务序列/回滚;write_json_atomic;磁盘预检;tmp 清理;manifest 校验;doctor
5. spec §11 验收 1-5 逐条可执行(真机向导走通留人工验收,断电/续传/只读/doctor 有自动化测试)
6. 251 现有测试零回归贯穿全程;最终 TOOL_VERSION=0.6.0
